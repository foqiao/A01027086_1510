import random

def two_jars():
    x = random.randint(1, )
    jar_one = 3 / 4 * 300
    jar_two = 1 / 4 * x
    if x > 0:
        x = int(jar_one) / (1 / 4)
        return x
    else:
        return None
two_jars()